#This is an example of a README that demonstrates the connection between WSL and GitHub via this repository and the versions code.

#Functions:

#git status

#git log -> to close press "q" or for more details use: --oneline / --stat / -p / --author / --grep / --graph

#git init -> to reinitialized too

#git clone -> to copy the remote repository

#git add "README.md" (or "." to add all files)

#git commit -m "Description commit"

#git branch -M main

#git remote add origin https://github.com/luiz-ed-ferreira/Project_LearnGit.git

#git push -u origin main

#git pull -u origin main

#ls -> list contents of the current folder

#ls -la -> list contents with details, including hidden files

#Navigation:

#git checkout main (or id commit or branch)

#git revert -> Create a new commit and discard the most recent changes (preserving the history) (:wq to close)

#git reset -> Discard the most recent commit and return to the previous commit

#Branchs:

#git branch header

#git checkout header